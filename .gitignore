# SECRETS — krytyczne, NIGDY nie commitować
.env
.env.*
*.env

# Strava MCP (vendored klon zewnętrznego repo z własnym git, plus zawiera .env z tokenami)
strava-mcp/

# Data files — w prywatnym repo zachowane
# Wykluczamy tylko temp / scratch
data/*.tmp
data/scratch/

# Claude local settings (per-machine, nie per-project)
.claude/settings.local.json

# OS / editor
.DS_Store
.vscode/
.idea/
*.swp

# Node / Python
node_modules/
__pycache__/
*.pyc
dist/
build/

# Netlify deploy credentials
.netlify-deploy.env
